\documentclass[tikz, border=1pt]{standalone}

\usepackage{tikz}

\usepackage{physics}

\usepackage{xcolor}
\definecolor{TurquoiseGreen}{HTML}{B8D5B8}
\definecolor{TealBlue}{HTML}{598392}
\definecolor{YellowOrange}{HTML}{FFB140}
\definecolor{RichBlack}{HTML}{011627}

\usetikzlibrary{shapes,arrows,positioning,calc,decorations.text}
\begin{document}
\pagestyle{empty}


\tikzstyle{function} = [circle, draw, fill=TurquoiseGreen, text=RichBlack, text width=5.5em, text centered]
\tikzstyle{block} = [rectangle, draw, fill=TurquoiseGreen, text=RichBlack, minimum width=6em, text centered,  minimum height=4em, align=center]
\tikzstyle{derived} = [rectangle, draw, rounded corners, fill=YellowOrange!33, text=RichBlack, minimum width=6em, text centered,  minimum height=4em, align=center]
\tikzstyle{check} = [text=TealBlue, align=center]

\tikzstyle{line} = [draw, very thick, color=RichBlack, ->]
\tikzset{>=latex}

\def\myshift#1{\raisebox{-2.5ex}}

\begin{tikzpicture}[auto]

    \node [block] (decision) at (4, 3) {Candidate decisions\\{\color{TealBlue}$\qty{x_1, x_2, \ldots, x_I}$}};
    \node [block] at (-4, 3) (scenarios) {Many possible futures\\{\color{TealBlue}$\qty{s_1, \ldots, s_J} \in \Omega$}};
    \node [function] (model) at (0, 0) {Deterministic system model\\{\color{TealBlue}$f(x, s)$}};
    \node [derived] at (0, -3) (smetrics) {Scenario metrics\\{\color{TealBlue}$u_{ij} = f(x_i, s_j)$}};
    \node [block] (prior) at (-4, -6) {Prior over scenarios\\{\color{TealBlue}$p(s)$}};\
    \node [derived] (weights) at (0, -6) {Scenario weights\\{\color{TealBlue}eq.~XX}};
    \node [derived] (combined) at (4, -6) {Distribution over\\outcomes\\{\color{TealBlue}$\hat{p}(u | x_i)$}};
    \node [derived] (dmetrics) at (8, -6) {Decision metrics\\{\color{TealBlue}$\hat{m}(x_i)$}};

    \path [line] (decision) -- (model);
    \path [line] (scenarios) -- (model);
    \path [line] (model) to (smetrics);
    \path [line] (scenarios.south) to[out=-90, in=135] (weights.north west);
    \path [line] (prior) -- (weights);
    \path [line] (smetrics) to (combined);
    \path [line] (weights) -- (combined);
    \path [line] (combined) to (dmetrics);

    \node [check] at (-9, 3) (span) {Do scenarios span\\plausible outcomes?};
    \path [line, dashed] (scenarios) -- (span);
    \node [check] at (9, 3) (represented) {Are relevant levers\\represented?};
    \path [line, dashed] (decision) -- (represented);
    \node [check] at (5, 0) (interpretable) {Is the model credible?\\Interpretable?};
    \path [line, dashed] (model) -- (interpretable);
    \node [check] at (5, -3) (discovery) {What are relevant regions\\of the $\qty(s, x)$ space?};
    \path [line, dashed] (smetrics) -- (discovery);
    \node [check] at (-4, -8) (priorcheck) {Are these consistent\\with prior knowledge?};
    \path [line, dashed] (prior) -- (priorcheck);
    \node [check] at (0, -8) (weightcheck) {Are computational\\issues apparent?};
    \path [line, dashed] (weights) -- (weightcheck);
    \node [check] at (4, -8) (ppc) {Are these consistent\\with our expectations?};
    \path [line, dashed] (combined) -- (ppc);

\end{tikzpicture}

\end{document}